# Task for config i3wm
-[x] Change terminal emulator using `kitty`
  -[x] *(bonus)* make it transparent in i3wm.
-[x] Change wallpaper for i3 (using `feh`)
-[x] Fix status bar (using `polybar`)
-[] Change to better font.
-[] Fix the window display for better appearance.
